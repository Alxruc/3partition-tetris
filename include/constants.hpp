#ifndef CONSTANTS_H
#define CONSTANTS_H

namespace constants
{
    constexpr int DEFAULT_BLOCK_SIZE = 50;
    constexpr int ADJUSTING_TIME = 500; //time we have to decide on a move (in ms)
}
#endif